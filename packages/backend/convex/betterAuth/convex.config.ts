import { defineComponent } from 'convex/server';

// Same name as the published component so existing auth data is preserved.
const component = defineComponent('betterAuth');

export default component;
