{
  "name": "@compound/dapi",
  "version": "0.1.0",
  "description": "The Compound tool catalog: every tool the Compound app exposes to agents and the CLI, as a name, a description, and zod input/output schemas. The one definition the app serves, the CLI derives its commands from, and the renderer validates against. Depends on zod and the Time parser only.",
  "license": "MPL-2.0",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check": "tsc --noEmit",
    "test": "bun test"
  },
  "keywords": [
    "compound",
    "mcp",
    "zod"
  ],
  "dependencies": {
    "@compound/jsx": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "@types/node": "^24.10.1",
    "typescript": "~5.9.3"
  }
}
